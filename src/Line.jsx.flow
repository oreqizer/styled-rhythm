/* @flow */
import * as React from 'react';

type Props = {
  as?: string | React.ComponentType<*>,
  theme: {
    baseFont: number,
    baseLineScale: number,
  },
  fontSize: number,
  marginTop: number,
  marginBottom: number,
  border?: number,
}

type Line = React.ComponentType<Props>;

declare export default Line;
