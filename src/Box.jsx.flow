/* @flow */
import * as React from 'react';

type Props = {
  as?: string | React.ComponentType<*>,
  theme: {
    baseFont: number,
    baseLineScale: number,
  },
  height: number,
  marginTop: number,
  marginBottom: number,
}

type Box = React.ComponentType<Props>;

declare export default Box;
